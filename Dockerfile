# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build putra-t-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t putra-t-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system putra-t-api && \
          adduser --system -G putra-t-api putra-t-api

COPY dist/putra-t-api putra-t-api
COPY prisma ./prisma/
RUN chown -R putra-t-api:putra-t-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix putra-t-api --omit=dev -f install

CMD [ "node", "putra-t-api" ]
